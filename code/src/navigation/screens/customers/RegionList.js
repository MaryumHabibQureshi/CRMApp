import React from "react";
import { ScrollView, SafeAreaView, Text } from "react-native";
import RegionList from "../../../features/customer/RegionList";

const RegionListScreen = () => (
  <SafeAreaView>
    <RegionList />
  </SafeAreaView>
);

export default RegionListScreen;
