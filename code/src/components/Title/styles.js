import { StyleSheet } from "react-native";

const stylesFn = () => {
  return StyleSheet.create({
    title: {
      fontSize: 20,
      fontWeight: "bold",
      alignSelf: "center",
    },
  });
};

export default stylesFn;
